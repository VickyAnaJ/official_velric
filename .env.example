# Local environment template for Velric architecture bootstrap
# Fill these before running local services.

APP_ENV=development
LOG_LEVEL=info

# LLM provider (Gemini API quickstart uses the same env var name)
GEMINI_API_KEY=

# Infrastructure placeholders
AWS_REGION=us-east-1
TF_STATE_BUCKET=
TF_LOCK_TABLE=
